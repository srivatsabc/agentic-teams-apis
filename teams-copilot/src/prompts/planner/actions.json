[
    {
        "name": "createTask",
        "description": "Create a new task with title and description",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "The title of the task to create"
                },
                "description": {
                    "type": "string",
                    "description": "The detailed description of the task to create"
                }
            },
            "required": [
                "title",
                "description"
            ]
        }
    },
    {
        "name": "deleteTask",
        "description": "Delete an existing task",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "The title of the task to delete"
                }
            },
            "required": [
                "title"
            ]
        }
    },
    {
        "name": "searchWeb",
        "description": "Search the web for current information, news, facts, or answers to questions",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "The search query to look up on the web"
                }
            },
            "required": [
                "query"
            ]
        }
    },
    {
        "name": "checkIncidentStatus",
        "description": "Check the status of an incident by calling the incident management API. Use this when users ask about incident status, mention incident IDs, or want updates on specific incidents.",
        "parameters": {
            "type": "object",
            "properties": {
                "incident_id": {
                    "type": "string",
                    "description": "The incident ID to check (e.g., INC0012345, INC-001234)"
                },
                "query": {
                    "type": "string",
                    "description": "The user's specific question or query about the incident"
                }
            },
            "required": [
                "incident_id",
                "query"
            ]
        }
    }
]